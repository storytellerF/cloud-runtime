services:
  android-dev:
    build: 
      context: .
      dockerfile: default.Dockerfile
    volumes:
      - android-dev-storage:/home/
    ports:
      - 8198:8080

volumes:
  android-dev-storage: